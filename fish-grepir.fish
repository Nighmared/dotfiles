#recursive case insensitive
alias grepir="grep -ir"
#recursive with context
alias grepirc="grep -irC 3"

#case insensitive
alias grepi="grep -i"
#case insensitive with context
alias grepic="grep -iC 3"
